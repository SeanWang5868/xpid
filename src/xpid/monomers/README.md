This package namespace is reserved for lightweight Xpid monomer-library data.

At runtime Xpid uses the CCP4 monomer library. It first searches local CCP4
environment paths, then downloads the CCP4 library from:

https://github.com/MonomerLibrary/monomers

Downloaded files are stored in the user cache, not in this installed package
directory.
